#include "main.h"
/**
 * positive_or_negative - function to print 0
 *
 * @i: parameter of the function
 *Return: always 0
 */

void positive_or_negative(int i)
{
	if (i < 0)
	{
		printf("%d is positive\n", i);
	}
	else
		printf("%d is zero\n", i);
}
