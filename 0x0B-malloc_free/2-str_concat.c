#include "main.h"


char *str_concat(char *s1, char *s2)
{
	int i;
	int j;
	char *s

	if ( s1 == NULL)
		s1 = "";

	if (s2 == NULL)
		s2 = "";

	s = malloc(ssize * sizeof(s1 + s2 +1);
			if (s == NULL)
			return (NULL);

			if (i = 0; i < s; i++)
			s = s1[i];

			if (j = 0; j < s;)
			s = s2[j];

